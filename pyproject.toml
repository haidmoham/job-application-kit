[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "job-application-kit"
version = "0.1.0"
description = "Candidate-agnostic support infrastructure for agent-assisted job applications"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["job_application_kit"]
